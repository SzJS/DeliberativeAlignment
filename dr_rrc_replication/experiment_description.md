The point of this experiment is to see what happens if we try to replicate the RRC paper (see /../project_description.md) but with a trained model instead of a prompted one. That is, I want to see what happens if we SFT on the data they generated (see their experimental data here: https://github.com/mint-philosophy/RRC_experiments).