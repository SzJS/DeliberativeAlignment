"""STUB — CLI driver: run every (task x arm) via inspect's eval_set (pipeline stage 5 of 5).

Usage
    uv run python evals/run_evals.py --config config.yaml
    uv run python evals/run_evals.py --config config.yaml --arms sdf_only --tasks spec_recall

Contract
    Build the explicit task x arm PAIRING and hand it to eval_set.

    DO NOT pass `tasks=[...]` and `model=[...]` together — eval_set forms the full tasks x models
    CROSS-PRODUCT, so every arm-tagged task would also run against every other arm's model, and
    `sdf_sft`-labelled results would be generated by the base model. Bind the model to each task
    at construction instead:

        pairs = [(t, a) for a in arms for t in task_fns]        # explicit, one model per task
        tasks = [task_with(fn(arm=a), model=model_string(a, cfg)) for fn, a in pairs]
        success, logs = eval_set(tasks=tasks, log_dir=cfg["eval"]["log_dir"])

    Check the inspect_ai version's actual API for binding a model to a task (`task_with`,
    `Task(model=...)`, or an equivalent) — the point is one model per task object, never a
    cross-product.

Why eval_set rather than eval
    log_dir is the RESUMPTION anchor: completed samples are reused across re-runs, so re-running
    a finished set is a no-op and a crash costs only the incomplete samples. This is the same
    resumability discipline as the generation cache, and it is why the eval stage needs no stamp
    logic of its own.

Invariants
    - Task x arm pairing is EXPLICIT (see above). `base` and `spec_in_context` additionally share
      a model string, so they cannot be told apart by model at all — `arm` must be a task
      argument, which is also what puts the arm name in the log for report.py to group on.
    - Verify every arm's merged checkpoint exists BEFORE launching. Missing checkpoints must fail
      fast — inspect would otherwise happily evaluate whatever path resolves and label the
      results with the wrong arm.

TODO
    - argparse: --config, --set, --arms, --tasks, --limit.
    - Under cfg.eval.serve == "vllm": start one server per distinct model path and reuse it
      across tasks. Loading an 8B model per task is most of the wall clock otherwise.
    - Call eval_lib.report.build_report at the end.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
